//main_grid = Array()

for (let i=0; i<27; i++) {
    //main_grid.push(new Array())
    for (let j=0; j<7; j++){
        //main_grid[i].push(document.getElementById("betgrid-"+i+"-"+j))
        document.getElementById("betgrid-"+i+"-"+j).addEventListener("click", () => {
            
        })
    }
}